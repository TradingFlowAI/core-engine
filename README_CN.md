<div align="center">

# ⚡ TradingFlow

### 开源的 DeFi 交易工作流引擎

[![License](https://img.shields.io/badge/License-可持续使用-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/Python-3.11+-green.svg)](https://python.org)
[![Build](https://img.shields.io/badge/Build-Passing-brightgreen.svg)]()

**构建透明、可组合、高性能的去中心化交易工作流**

[快速开始](#-快速开始) • [系统架构](#-系统架构) • [节点目录](#-内置节点) • [参与贡献](#-参与贡献)

</div>

---

## 🌟 为什么选择 TradingFlow？

在快速发展的加密世界中，交易者和开发者需要**透明**、**可扩展**、**易维护**的工具。TradingFlow 是一个开源的 DAG 工作流引擎，专为 DeFi 操作设计——让您完全掌控并清晰了解交易逻辑的每一步。

### ✨ 核心理念

| 理念 | 描述 |
|------|------|
| 🔍 **透明** | 每个节点执行都有日志可追溯。没有黑箱——清楚看到每一步发生了什么 |
| 📈 **水平扩展** | 无状态 Worker + Redis 协调。从 1 个实例无缝扩展到 100+ 个 |
| ⚡ **高性能** | 异步优先架构，节点执行延迟 < 50ms (p99) |
| 🛠️ **易于维护** | 清晰的关注点分离。模块化节点。完善的日志和监控 |
| 🔗 **原生多链** | 内置支持 Aptos、Flow EVM 及 EVM 兼容链 |
| 🧩 **可组合** | 15+ 节点类型自由组合，构建复杂交易策略 |

---

## 🏗️ 系统架构

```
                                    ┌─────────────────────────────────────┐
                                    │           TradingFlow               │
                                    │           (核心引擎)                 │
                                    └─────────────────────────────────────┘
                                                    │
                    ┌───────────────────────────────┼───────────────────────────────┐
                    │                               │                               │
                    ▼                               ▼                               ▼
            ┌─────────────┐                 ┌─────────────┐                 ┌─────────────┐
            │  数据节点   │                  │  处理节点   │                  │  交易节点   │
            │  (Binance,  │   ──信号──►     │  (AI, 代码, │   ──信号──►     │   (Swap,    │
            │ GeckoTerm.) │                 │    模型)    │                 │   Vault)    │
            └─────────────┘                 └─────────────┘                 └─────────────┘
                    │                               │                               │
                    └───────────────────────────────┼───────────────────────────────┘
                                                    │
        ┌───────────────────────────────────────────┼───────────────────────────────────────────┐
        │                                           │                                           │
        ▼                                           ▼                                           ▼
┌───────────────┐                         ┌─────────────────┐                         ┌─────────────────┐
│     Redis     │                         │    RabbitMQ     │                         │   PostgreSQL    │
│ ─────────────  │                         │  ─────────────  │                         │  ─────────────  │
│ • 状态存储    │                         │ • 任务队列     │                         │ • 执行日志     │
│ • 发布/订阅   │                         │ • 事件总线     │                         │ • Flow 历史    │
│ • 信号缓存    │                         │ • 分布式消息   │                         │ • 数据分析     │
│ • 分布式协调  │                         │                │                         │                │
└───────────────┘                         └─────────────────┘                         └─────────────────┘
        │                                           │                                           │
        └───────────────────────────────────────────┼───────────────────────────────────────────┘
                                                    │
                                    ┌───────────────┴───────────────┐
                                    │                               │
                                    ▼                               ▼
                            ┌─────────────┐                 ┌─────────────┐
                            │  Worker 1   │      ...        │  Worker N   │
                            │  (无状态)   │                 │  (无状态)   │
                            └─────────────┘                 └─────────────┘

                            ▲ 水平扩展 - 按需添加 Worker 实例 ▲
```

### 为什么采用这个架构？

| 组件 | 角色 | 扩展性优势 |
|------|------|-----------|
| **Redis** | 分布式状态、发布/订阅信号、缓存的协调中心 | 实现无状态 Worker；支持 Redis Cluster 高可用 |
| **RabbitMQ** | 可靠的任务分发和事件消息 | 解耦生产者/消费者；处理反压 |
| **PostgreSQL** | 执行日志和分析数据的持久化存储 | 查询历史数据；合规与审计 |
| **Workers** | 处理节点的无状态执行单元 | 水平扩展；零停机部署 |

### 核心组件

| 组件 | 功能 |
|------|------|
| **Flow 调度器** | 编排 DAG 执行，支持周期管理、故障恢复和多实例协调 |
| **节点执行器** | 管理节点生命周期、超时处理和信号路由 |
| **信号系统** | 类型安全的信号传播，支持 Redis 持久化 |
| **金库服务** | Aptos、Flow EVM 等链的交易集成 |
| **任务管理器** | 多进程任务协调与分布式状态管理 |

---

## 🚀 快速开始

### 环境要求

- Python 3.11+
- Redis 6+
- RabbitMQ 3.x（可选，分布式模式需要）
- PostgreSQL 14+（日志持久化需要）

### 安装步骤

```bash
# 克隆仓库
git clone https://github.com/TradingFlowAI/core-engine.git
cd core-engine

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt
```

### 启动基础设施

```bash
# 启动 Redis
docker run -d --name redis -p 6379:6379 redis:alpine

# 启动 PostgreSQL
docker run -d --name postgres \
  -p 5432:5432 \
  -e POSTGRES_USER=tradingflow \
  -e POSTGRES_PASSWORD=tradingflow \
  -e POSTGRES_DB=tradingflow \
  postgres:15-alpine

# 启动 RabbitMQ（可选，分布式模式需要）
docker run -d --name rabbitmq \
  -p 5672:5672 -p 15672:15672 \
  -e RABBITMQ_DEFAULT_USER=guest \
  -e RABBITMQ_DEFAULT_PASS=guest \
  rabbitmq:3-management
```

### 运行 TradingFlow

```bash
python server.py
```

引擎将在 `http://localhost:7002` 启动。

---

## 📦 内置节点

TradingFlow 提供丰富的生产就绪节点：

### 📈 数据节点
| 节点 | 描述 |
|------|------|
| `binance_price_node` | 从 Binance 获取实时和历史价格数据 |
| `price_node` | 多源价格聚合，支持缓存 |
| `rootdata_node` | 从 RootData 获取链上数据 |
| `gsheet_node` | Google Sheets 数据读写集成 |
| `dataset_node` | 加载和处理自定义数据集 |

### 🤖 处理节点
| 节点 | 描述 |
|------|------|
| `ai_model_node` | 运行 AI/ML 模型生成信号 |
| `code_node` | 沙箱环境中执行自定义 Python 代码 |
| `x_listener_node` | 监控 Twitter/X 获取社交信号 |
| `rsshub_node` | RSS 订阅聚合与过滤 |

### 💱 交易节点
| 节点 | 描述 |
|------|------|
| `swap_node` | Aptos、Flow EVM 上的 DEX 交换，内置滑点保护 |
| `buy_node` | 简化的买入操作，指定基础代币 |
| `sell_node` | 简化的卖出操作，指定目标代币 |
| `vault_node` | 直接与金库合约交互 |

### 📢 输出节点
| 节点 | 描述 |
|------|------|
| `telegram_sender_node` | 向 Telegram 频道发送通知 |

---

## 🔌 API 参考

### Flow 管理

```bash
# 执行新的 Flow
curl -X POST http://localhost:7002/flows/execute \
  -H "Content-Type: application/json" \
  -d '{
    "flow_id": "my_strategy",
    "cycle_interval": "5m",
    "flow_json": {
      "nodes": [...],
      "edges": [...]
    }
  }'

# 获取 Flow 状态
curl http://localhost:7002/flows/my_strategy/status

# 停止运行中的 Flow
curl -X POST http://localhost:7002/flows/my_strategy/stop
```

### 节点执行

```bash
# 执行单个节点
curl -X POST http://localhost:7002/nodes/execute \
  -H "Content-Type: application/json" \
  -d '{
    "flow_id": "test_flow",
    "node_id": "price_check",
    "node_type": "binance_price_node",
    "config": {
      "symbol": "BTCUSDT",
      "interval": "1h"
    }
  }'

# 检查节点状态
curl http://localhost:7002/nodes/price_check/status
```

### 健康检查

```bash
curl http://localhost:7002/health
```

---

## 📊 性能与可扩展性

| 指标 | 数值 |
|------|------|
| 节点执行延迟 | < 50ms (p99) |
| 信号传播时间 | < 10ms |
| 单 Worker 并发 Flow | 100+ |
| 水平扩展 | 无限 Worker |
| 单 Worker 内存占用 | ~256MB |

### 扩展指南

```bash
# 单实例（开发环境）
python server.py

# 多 Worker（生产环境）
# Worker 1
WORKER_ID=worker-1 python server.py --port 7002

# Worker 2
WORKER_ID=worker-2 python server.py --port 7003

# Worker N...
WORKER_ID=worker-n python server.py --port 700X
```

所有 Worker 通过 Redis 共享状态，从 RabbitMQ 接收任务，实现真正的水平扩展。

---

## 🛠️ 扩展 TradingFlow

### 创建自定义节点

```python
from nodes.node_base import NodeBase

class MyCustomNode(NodeBase):
    """
    以你独特的方式处理数据的自定义节点。
    """
    
    # 定义输入/输出句柄
    INPUTS = {
        "data_in": {"type": "any", "description": "输入数据"}
    }
    
    OUTPUTS = {
        "result_out": {"type": "any", "description": "处理结果"}
    }
    
    async def execute(self) -> dict:
        # 从连接的节点获取输入
        input_data = await self.get_input("data_in")
        
        # 你的自定义逻辑
        result = self.process(input_data)
        
        # 发射输出信号
        await self.emit_signal("result_out", result)
        
        return {"status": "success", "result": result}
```

### 注册你的节点

```python
# 在 core/init_builtin_nodes.py 中
from nodes.my_custom_node import MyCustomNode

def init_builtin_nodes():
    # ... 现有节点 ...
    NodeRegistry.register("my_custom_node", MyCustomNode)
```

---

## 🧪 测试

```bash
# 运行所有测试
pytest tests/

# 运行特定测试模块
pytest tests/test_node_base_status.py -v

# 运行带覆盖率报告的测试
pytest tests/ --cov=. --cov-report=html
```

---

## 📖 文档

- [节点开发指南](docs/)
- [信号系统设计](nodes/docs/SIGNAL_SYSTEM_DESIGN.md)
- [Swap 节点文档](nodes/docs/swap_node_doc.md)
- [积分集成](docs/CREDITS_INTEGRATION.md)
- [Quest 集成](docs/QUEST_INTEGRATION_STATION.md)

---

## 🤝 参与贡献

我们欢迎社区贡献！以下是参与方式：

1. **Fork** 本仓库
2. **创建** 功能分支 (`git checkout -b feature/amazing-node`)
3. **提交** 你的更改 (`git commit -m 'Add amazing node'`)
4. **推送** 到分支 (`git push origin feature/amazing-node`)
5. **发起** Pull Request

提交前请阅读我们的 [贡献指南](CONTRIBUTING.md)。

---

## 🗺️ 路线图

- [ ] WebSocket 实时更新
- [ ] 更多 DEX 集成（Uniswap V4、Curve、Balancer）
- [ ] 策略回测框架
- [ ] 可视化流程构建器集成
- [ ] 社区节点插件市场
- [ ] Kubernetes Helm Charts 生产部署方案

---

## 📜 开源协议

本项目采用 **TradingFlow 可持续使用许可证** - 详见 [LICENSE](LICENSE) 文件（[中文版](LICENSE_CN.md)）。

### 许可证摘要

| 使用场景 | 是否需要许可？ |
|----------|----------------|
| 👤 个人使用 | ❌ **免费** |
| 🎓 非营利 / 教育用途 | ❌ **免费** |
| 🏢 小型企业（收入/融资 <$1M），仅内部使用 | ❌ **免费** |
| 🏛️ 收入或融资 ≥$1M 的企业 | ✅ 需要商业许可 |
| 🌐 为第三方提供服务（任何规模） | ✅ 需要商业许可 |
| ☁️ SaaS / 托管服务 | ✅ 需要商业许可 |

**简而言之**：对个人和小团队免费。企业和服务提供商需要商业许可。

商业许可咨询请联系：[cl@tradingflows.ai](mailto:cl@tradingflows.ai)

---

## 🔗 相关链接

- 🌐 [官网](https://tradingflows.ai)
- 📚 [文档中心](https://docs.tradingflows.ai)
- 💬 [Telegram](https://t.me/tradingflowai)
- 🐦 [Twitter](https://twitter.com/TradingFlowAI)
- 📧 [联系我们](mailto:cl@tradingflows.ai)
- 🐙 [GitHub](https://github.com/TradingFlowAI)

---

## 👥 贡献者

- **[@Morboz](https://github.com/Morboz)** - 核心作者，负责设计并实施初始架构
- **[CL](https://github.com/TheCleopatra)** - 后续进行了长达半年的维护与收尾工作

---

<div align="center">

**由 TradingFlow 团队用 ❤️ 打造**

*赋能去中心化世界的透明、可扩展交易*

</div>
